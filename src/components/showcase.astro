---
import ContentSection from "~/components/content-section.astro";
import ShowcaseCard from "~/components/showcase-card.astro";
import sites from "~/data/showcase/sites.json";
---

<ContentSection title="Inscripciones" id="inscripciones">
  <Fragment slot="lead">
    Inscríbete a la <span class="text-primary">próxima fecha</span> {" "}
    <span class="text-primary">desde</span> el siguiente formulario {" "}
    <br/>
    <br/>
    <a href="https://www.google.com/"> <span class="text-primary">Click aquí</span> </a>
  </Fragment>
  <!-- <div class="max-w-6xl space-y-2">
    <div class="grid grid-cols-1 gap-4 md:grid-cols-2 lg:grid-cols-3">
      {
        sites.map(({ title, image, url }) => (
          <ShowcaseCard title={title} image={image} url={url} />
        ))
      }
    </div>
    <p class="text-sm text-right">
      <a class="text-primary" href="https://astro.build/showcase/">
        ...and more &rarr;
      </a>
    </p>
  </div> -->
</ContentSection>
